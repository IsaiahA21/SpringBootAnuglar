/**
 * I had to generate this file because Angular stop doing it automatically
 * This file is used to store the environment variables and will be replaced during the build using the `fileReplacements` array in the angular.json file
 * ng build --prod replaces the environment.ts file with the environment.development.ts file
 */
export const environment = {
    production: false,
    API_BASE_URL: 'http://localhost:8080'
};
